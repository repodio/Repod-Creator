import React from "react";

const ConsoleEpisodes = () => {
  // const shows = useSelector(showsSelectors.getClaimedShows);

  // if (!show) {
  //   return (
  //     <div className="m-8 opacity-50">
  //       <Loader />
  //     </div>
  //   );
  // }

  return (
    <div className="flex flex-col justify-start items-start mr-12">
      <p className="text-2xl font-bold text-repod-text-primary">episodes</p>
    </div>
  );
};

export default ConsoleEpisodes;
